int insertionSort(a, size) {
     int pivot=1;
     while(pivot<size) {
          temp = a[pivot];
          i = pivot - 1;
          while (i>=0 && a[i] > temp)
              a[i+1]=a[i];
              i--;
     }
     a[i+1]=temp;
     pivot++;
}
